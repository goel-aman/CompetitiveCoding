#include<bits/stdc++.h>
using namespace std;

class Solution {
public:
    int numSequence(string s,int used,int sLength){

    }

    int numPermsDISequence(string S) {
        int sLength = S.length();
        int used = 0;
        return numSequence(s,used,sLength);
    }
};